---
- name: fetch latest git version
  git:
    repo: "https://github.com/j-chimienti/pollofeed.git"
    update: yes
    dest: "./{{APP_DIR}}"


- name: cron
  include: cron.yml


- name: env files
  when: false
  include: copyEnv.yml


- name: reboot
  when: false
  include: reboot.yml
